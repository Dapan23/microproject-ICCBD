apiVersion: v1
kind: Pod
metadata:
  name: iperf3-server
  labels:
    app: iperf3-server
spec:
  nodeName: worker-01
  containers:
  - name: iperf3
    image: alpine:3.19.1
    command: ["/bin/sh", "-c", "apk add --no-cache iperf3 && iperf3 -s"]
    ports:
    - containerPort: 5201
